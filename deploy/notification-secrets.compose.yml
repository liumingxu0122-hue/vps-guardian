services:
  controller:
    volumes:
      - type: bind
        source: ${GUARDIAN_NOTIFICATION_SECRETS_DIR:?set GUARDIAN_NOTIFICATION_SECRETS_DIR}
        target: /run/notification-secrets
        read_only: true
        bind:
          create_host_path: false
